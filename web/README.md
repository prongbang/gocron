# gocron web

UI for the gocron scheduler API (`GOCRON_API=true`): list, create, and stop jobs.

```shell
bun install
PUBLIC_GOCRON_API=http://localhost:8000 bun run dev
```

`PUBLIC_GOCRON_API` defaults to `http://localhost:8000`.
